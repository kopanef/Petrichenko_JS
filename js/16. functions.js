
/*
"use strict";

let num = 20;

function showFirstMessage(text) {
  console.log(text);
  num = 10;
}

// Вызов функции
showFirstMessage('Hello World!');
console.log(num);

function calc(a, b) {
  return (a + b);
}

console.log(calc(4, 3));
console.log(calc(5, 6));
console.log(calc(10, 6));

function ret() {
  let num = 50;
  //
  return num;
}

const anotherNum = ret();
console.log(anotherNum)

// Второй вид переменной (Экспрешен)
// Работает только после объявления
const logger = function () {
  console.log("Hello")
};

logger()

// Стрелочная функция
// Не имеет своего контекста

// слева — аргументы; справа — сама функция
const calc = (a, b) => a + b;

// Если один аргумент
const calc = a => a + b;

const calc = (a, b) => { return a + b };

const calc = (a, b) => {
  console.log('1');
  return a + b;
};

*/